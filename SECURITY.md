# 安全说明

## 报告漏洞

若你认为发现了 **Patchbay 自身的安全漏洞**（例如鉴权绕过、远程代码执行、敏感数据泄露），请 **不要** 在公开 Issue 中披露细节。

请通过以下方式之一联系维护者（择一即可）：

- GitHub **Security advisories**（若仓库已开启 *Private vulnerability reporting*，优先使用）。  
- 向仓库维护者发送 **私信渠道**（若你在其它社区已有联系方式）。

请在报告中尽量包含：影响版本或 commit、复现步骤、潜在影响范围。

## 不在此列的问题

- **Telegram / Discord 平台账号被封**、Bot Token 泄露因用户误提交到公开仓库等，属于使用与密钥管理问题，请优先轮换 Token 并检查 `.gitignore`。  
- **第三方依赖**的漏洞请同时关注上游 CVE；我们会在收到确认后尽快升级依赖。

## 披露策略

维护者确认有效后，会安排修复与发布说明；在修复可用前请配合 **不提前公开** 可利用细节（协调披露）。
